See https://github.com/Atten4Vis/ConditionalDETR for the Installation
The pre-trained model (DETR-DC5-R50) on COCO dataset is needed. Specify the args.resume as the downloaded model path
The GAIC dataset is needed. Specify the args.dataset_root as the downloaded dataset path.